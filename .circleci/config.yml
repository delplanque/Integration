version: 2
jobs:
 build:
  machine: true
  steps:
    - checkout
    - run: docker login -u jordandelplanque -p ascasc0123
    - run: docker build -t jordandelplanque/webserve:firsttry .
    # deploy the image
    # - run: docker push pagepersos
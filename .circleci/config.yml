version: 2
jobs:
 build:
   machine: true
   environment:
     IMAGE_NAME: jordandelplanque/webserve
   docker:
     - image: jordandelplanque/webserve:firsttry
   steps:
     - checkout
     - run: docker build -t jordandelplanque/webserve:firsttry .
     # deploy the image
     # - run: docker push pagepersos